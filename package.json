{
    "name": "story-explorer-vanilla-spa",
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build && node build-sw.js",
        "preview": "vite preview"
    },
    "devDependencies": {
        "vite": "^5.0.0",
        "workbox-cli": "^7.0.0"
    },
    "dependencies": {
        "leaflet": "^1.9.4",
        "workbox-build": "^7.3.0"
    }
}
